# Accessibility test examples

Open [`index.html`](./index.html) to review a compact set of focus, status,
preference, and approval-context examples. It is a manual test fixture, not an
accessibility certification.

Run the structural guard with:

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File scripts/Test-Accessibility.ps1
```
